import 'package:flutter/material.dart';

class AppColors {
  // ✨ Hệ màu nền chính (Theme Colors)
  static const Color background = Color(0xFFFAF8F5);      // Kem sữa Pastel ngọt ngào
  static const Color primary = Color(0xFFD4A373);         // Nâu trà sữa (Milk Tea Brown)
  static const Color primaryDark = Color(0xFF8B5A2B);     // Nâu đậm tiêu đề
  static const Color cardBackground = Colors.white;       // Nền ô nhập liệu / Khung chứa trắng sạch

  // 🍓 Hệ màu trạng thái Pastel
  static const Color success = Color(0xFFCCD5AE);         // Xanh Sage tươi mát (Thành công)
  static const Color error = Color(0xFFFFCAD4);           // Hồng dâu dịu nhẹ (Thất bại / Lỗi)
  static const Color errorAccent = Color(0xFFFF9B9B);     // Hồng đỏ đậm cho các lỗi nổi bật
}